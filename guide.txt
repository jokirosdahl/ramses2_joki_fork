module load nvhpc-hpcx-cuda12/25.5
cd bin
make NDIM=3 COMPILER=NVHPC PATCH=../gpu
cd ..
salloc --nodes=1 --ntasks=1 --time=01:00 --gres=gpu:1
Once on the compute node:
    bin/ramses3d namelist/sedov3d.nml
Debug:
    compute-sanitizer --tool memcheck --print-level info --report-api-errors all ./bin/ramses3d namelist/sedov3d.nml > sanitizer.log
Alias for making with different execution values:
mk() {     local exec_val=$1;     make clean && make NDIM=3 COMPILER=NVHPC PATCH=../gpu EXEC=${exec_val}_ramses TPB=${exec_val}; }