This directory contains tests that need a certain ramses sructure
in place to test one specific module, subroutine, etc... 
Failures therefore do not necessarily have to be caused by changes 
to the routines which are actually tested.

